#include <stdio.h>

int main()
{

    int vetor[101];

    int placa;
    int listar;
    int x;
    int count = 0;
    while (scanf("%d", &x) != EOF)
    {

        if (x == 1)
        {
            if(count == 100) {

            } else {
                scanf("%d", &placa);
                v[count]
            }
        }
        else if (x == 2)
        {

            scanf("%d", &listar);

            imprimeHeap(listar);
        }
    }

    return 0;
}